transcriber: openai_whisper          # Options: whisperx, openai_whisper
whisper_model: medium  # Options: tiny, small, medium, large, large-v2, large-v3-turbo
use_gpu: true

ai_analyzer:
  model: gemini-1.5-flash-latest  # Options: gemini-1.5-flash-latest, gemini-1.5-pro-latest
  prompt: viral_formula          # Options: viral_formula, infinite_loop

downloader:
  cookies_file: null     # Optional: path to cookies.txt (Netscape format)
                         # Example: cookies_file: "cookies.txt"

face_detector: yolo            # Options: yolo

notification:
  telegram:
    enabled: true

# downloader:
#   cookies_file: "cookies.txt"  # Path to your exported cookies